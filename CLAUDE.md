# Claude compatibility note

[`AGENTS.md`](./AGENTS.md) is the single authoritative agent guide for this repository. Read it
before changing code, then read the architecture and delivery documents it links. This pointer is
kept only so Claude-based tooling follows the same rules instead of maintaining a divergent copy.
