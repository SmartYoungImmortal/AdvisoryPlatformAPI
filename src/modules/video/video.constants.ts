export const VIDEO_MESSAGES = {
  accessGranted: 'Video access granted',
  accessUnavailable: 'Video access not found',
  roomUnavailable: 'Video room could not be created',
} as const;

export const VIDEO_ROOM_PREFIX = 'appointment';
